import { AdminEmptyState, AdminPageHeader, AdminShell } from "@/components/admin/AdminShell"; import { requireAdmin } from "@/lib/admin/auth";
export default async function Page(){const admin=await requireAdmin();return <AdminShell admin={admin}><AdminPageHeader eyebrow="Engagement" title="Events" description="Plan Purpose OS gatherings, workshops, cohorts, Hub events, and experience-linked sessions." action={<span className="admin-disabled-action">CREATE EVENT · ARCHITECTURE ONLY</span>}/><AdminEmptyState title="No event system is connected" description="Future events will support schedule, location, capacity, RSVP, access, attendance, organization or Hub, linked experience, and communications."/></AdminShell>}
