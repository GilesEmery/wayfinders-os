import { AdminEmptyState, AdminPageHeader, AdminShell } from "@/components/admin/AdminShell"; import { requireAdmin } from "@/lib/admin/auth";
export default async function Page(){const admin=await requireAdmin();return <AdminShell admin={admin}><AdminPageHeader eyebrow="Outreach" title="Communications" description="Prepare platform email, in-app announcements, and SMS campaigns from one operational view." action={<span className="admin-disabled-action">CREATE COMMUNICATION · COMING NEXT</span>}/><div className="admin-channel-tabs"><span className="is-active">Email</span><span>In-app announcements</span><span>SMS</span></div><AdminEmptyState title="No communication service is connected" description="Campaign history will show audience, status, sent date, delivery, opens, clicks, and replies when enabled."/></AdminShell>}
