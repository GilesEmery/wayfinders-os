import type { ThemeConfiguration } from "@/lib/experiences/builder/types";
import { resolveExperienceTheme } from "@/lib/experiences/builder/theme-resolver";

export function ThemePreview({ configuration, assetNames = {} }: { configuration: ThemeConfiguration; assetNames?: Record<string, string> }) {
  const theme = resolveExperienceTheme(configuration);
  const classes = ["experience-theme-preview", theme.typographyClass, theme.headingClass, theme.buttonClass, theme.cardClass, theme.navigationClass, theme.spacingClass, theme.cornerClass].join(" ");
  return <div className={classes} style={theme.style}><header><div><span>Purpose OS Experience</span><strong>{configuration.logoResourceId ? assetNames[configuration.logoResourceId] ?? "Selected logo" : "Purpose OS"}</strong></div><nav>Journey <b>Navigator</b></nav></header><section className="experience-preview-cover"><small>{configuration.coverImageResourceId ? `Cover · ${assetNames[configuration.coverImageResourceId] ?? "Selected resource"}` : "Experience Preview"}</small><h2>Activate your purpose.</h2><p>A representative preview of headings, body copy, navigation, cards, and actions.</p><div><button type="button">Continue</button><button type="button">Save for later</button></div></section><div className="experience-preview-grid"><article><small>Content card</small><h3>A meaningful next step</h3><p>Purpose OS helps each Wayfinder move from discovery toward thoughtful action.</p></article><aside><small>Callout</small><strong>Notice what is becoming clearer.</strong><p>Muted text provides supporting context without competing with the main idea.</p></aside></div>{configuration.decorativeResourceIds?.[0] && <footer>Decorative asset · {assetNames[configuration.decorativeResourceIds[0]] ?? "Selected resource"}</footer>}</div>;
}
