/**
 * Development-only access override for the Life Mapping U journey.
 *
 * Set this to false before production participant use. This flag bypasses only
 * navigation gating; it must never change prerequisites, completion, or stored
 * participant progress.
 */
export const LMU_DEV_UNLOCK_ALL = false;
